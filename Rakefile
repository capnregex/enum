# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'enum' do
  developer('capnregex', 'capnregex@gmail.com')

  # self.rubyforge_name = 'enumx' # if different than 'enum'
end

# vim: syntax=ruby
